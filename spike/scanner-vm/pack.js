// Moved to `harness/ts_scanner_pack.mjs`, which also gained the `decode` half.
// Kept as a re-export so the spike's workflow keeps working; see that file.
module.exports = require('../../harness/ts_scanner_pack.mjs');
