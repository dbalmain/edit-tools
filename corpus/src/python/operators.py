simple = a + b

arithmetic_chain = first_term + second_term + third_term + fourth_term + fifth_term

mixed_precedence = alpha + beta * gamma - delta / epsilon + zeta * eta - theta / iota

boolean_chain = condition_one and condition_two and condition_three and condition_four

or_chain = first_option or second_option or third_option or fourth_option or fifth

mixed_boolean = (alpha and beta) or (gamma and delta) or (epsilon and zeta) or theta

comparison = left_hand_side_value == right_hand_side_value != other_value < bound

chained_comparison = lower_bound <= candidate_value <= upper_bound_of_the_interval

unary = -value

not_expression = not some_condition_that_is_fairly_long_and_will_need_wrapping_here

membership = element in container_of_elements and other_element not in other_container

identity = first_object is second_object or third_object is not fourth_object_here

bitwise = flags_alpha | flags_beta & flags_gamma ^ flags_delta << shift_amount_here
