;; Numbers in several bases, booleans, characters, a vector (already
;; broken), keywords, the empty list. Spellings the grammar distinguishes.
(define decimal 42) ; integer

(define hex #x2a)

(define binary #b101010)

(define octal #o52)

(define real 3.14)

(define ratio 1/2)

(define true #t)

(define false #f)

(define vec
#(1
2
3
4
5))

(define key #:key)

(define none '())
