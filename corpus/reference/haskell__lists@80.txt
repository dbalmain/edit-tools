module Lists where

-- lists, tuples, ranges, comprehensions; empty already in canonical form
empty = [] -- the space-padded form lives in normalisation.hs

unit = ()

pair = (1, 2)

triple = (1, 2, 3)

short = [1, 2, 3]

broken =
  [ 1,
    2,
    3
  ]

range = [1 .. 10]

stepped = [1, 3 .. 9]

comp = [x * 2 | x <- [1 .. 10], even x] -- filter in a comprehension

nested = [[1, 2], [3, 4], [5, 6]]
