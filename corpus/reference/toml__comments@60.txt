# A comment before the first key.
title = "comments" # A trailing comment on a key-value pair.

values = [
  1, # A trailing comment inside an array.
  # An own-line comment inside an array.
  2,
  # A comment before the closing delimiter.
]

# A comment after a complete construct.
enabled = true
